OPENAI_API_KEY=your_api_key_here
# Optional fallback keys. Users can also enter a key for the current browser session in the UI.
GEMINI_API_KEY=
DEEPSEEK_API_KEY=
# Optional: use models available to your provider accounts.
OPENAI_MODEL=gpt-5.6-luna
GEMINI_MODEL=gemini-3.5-flash
DEEPSEEK_MODEL=deepseek-v4-flash
PORT=3000
